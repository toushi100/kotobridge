# kotobridge

## Setup with uv

```bash
uv python install 3.11
uv venv --python 3.11
source .venv/bin/activate
uv sync
```

Run:

```bash
uv run koto --help
uv run koto translate "/path/to/audio.mp3"
```
